# QuantLLM 核心依赖
# 安装: pip install -r requirements.txt

# 数据采集与处理
akshare>=1.18
pandas>=2.0
numpy>=2.0
openpyxl>=3.1
chinese-calendar>=1.9

# 模型训练
torch>=2.6
unsloth>=2025.3
trl>=0.16
transformers>=4.51
datasets>=4.0
peft>=0.18
bitsandbytes>=0.49
accelerate>=1.13

# 数据增强（可选，需要 ollama）
requests>=2.32
python-docx>=1.1

# 评估（可选）
# rouge-score>=0.1

# GGUF 导出（可选）
# llama-cpp-python>=0.3
