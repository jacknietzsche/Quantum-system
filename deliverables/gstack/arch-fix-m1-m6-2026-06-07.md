# M1-M6 架构评审问题解决方案（已应用）

**日期**: 2026-06-07
**场景**: 架构文档修订（M1-M6 全套 7 份）
**参与成员**: 软件工坊主理人 沽思航（Gu）
**原始评审报告**: `docs/architecture/designs/REVIEW-M3-M6.md`（25 项问题：7 严重、12 中等、6 轻微）

---

## 📌 TL;DR

- 整体结论：🟢 **已闭环**：25 项问题全部按用户提供的方案落实到 7 份设计文档
- 阻塞项数量：0（已逐项修缮）
- 下一步：进入 Phase 3 编码实施

---

## 🎯 核心结论卡片

| 项目 | 内容 |
|------|------|
| Go / No-Go | 🟢 **Go**（架构文档层） |
| 严重度分布 | 🔴 7 / 🟠 12 / 🟢 6（**全部已修**） |
| 关键行动项 | 5 项 P0 + 6 项 P1（详见下方"行动清单"） |
| 建议负责人 | software-engineer（Phase 3 编码） |

---

## 1. 各 P0/P1 严重问题落地清单

### 🔴 P0-1：`step.type` 枚举与 `confirm_type` 二级枚举冲突

**修改文件**：
- `M3-agent-architecture.md` §3.3：枚举扩展为 5 个值 `skill / agent / confirm_plan / confirm_email / wait_for_user`
- `M3-agent-architecture.md` §3.4：执行分支按 5 个枚举值分别处理
- `M3.5-agent-runtime-state.md` §1.2：Step 结构更新
- `M3.5-interrupt-mechanism.md` §3 / §6：interrup t_data 与 SSE 事件携带完整 `params`
- `M4-langgraph-graph.md` §4.7：interrupt_node 路由条件改为 5 选 1
- `M4-langgraph-graph.md` §8：日频分析计划示例改用 `confirm_plan / confirm_email`

### 🔴 P0-2：Checkpoint 保留时间冲突（1h vs 7d）

**修改文件**：
- `M1-infrastructure-and-config.md` §1.5：AgentRun 表新增 `checkpoint_retention_hours` 字段
- `M1-infrastructure-and-config.md` §1.10.1：新增"Phase 2 数据库清理任务"小节
- `M3.5-agent-runtime-state.md` §2.2：新增"Checkpoint 双段保留"小节（热 1h / 冷 7d）
- `M4-langgraph-graph.md` §7：状态清理与资源回收按双段保留
- `M6-frontend-architecture.md` §3.1：chatStore 新增 `canResume` 字段

### 🔴 P0-3：`universe_snapshot` checkpoint 排除机制

**修改文件**：
- `M3.5-agent-runtime-state.md` §1.3：`universe_snapshot` 字段说明更新（LangGraph transient reducer）
- `M3.5-agent-runtime-state.md` §2.3：新增"universe_snapshot 排除机制实现"小节
- `M1-infrastructure-and-config.md` §1.5：SkillExecution 表新增 `large_payload_ref` 字段

### 🔴 P0-4：`waiting_user` 状态时是否追加 system 消息

**修改文件**：
- `M3.5-agent-runtime-state.md` §1.1：`messages` 字段写入者增加 Interrupt 节点
- `M3.5-agent-runtime-state.md` §3.1：新增"system 消息注入时机"小节（4 个注入点）
- `M3.5-interrupt-mechanism.md` §3 / §8.1：interrupt_node 示例代码追加 system_msg_pause + user_action_msg
- `M4-langgraph-graph.md` §4.7.1：interrupt_node 完整代码包含 system 消息追加
- `M1-infrastructure-and-config.md` §1.5：新增 `conversation_history` 表
- `M1-infrastructure-and-config.md` §1.10：M1.10 任务卡片（启动时加载 + 节点追加）
- `M6-frontend-architecture.md` §3.1：messages role 包含 system

### 🟠 C-2：`idempotency_key` 前端感知

**修改文件**：
- `M1-infrastructure-and-config.md` §1.5：SkillExecution 表保持 `idempotency_key` UNIQUE 约束 + `replayed` 字段，新增 `step_type` 字段
- `M5-api-design.md` §3.5：`/api/reports/{date}/email` 描述补充幂等机制（基于 `report_date + email_to`）

### 🟠 C-3：SkillRegistry 按 intent 过滤

**修改文件**：
- `M1-infrastructure-and-config.md` §1.5：新增 `skill_registry` 表
- `M1-infrastructure-and-config.md` §1.10：M1.9 任务卡片（启动时填充）
- `M3-agent-architecture.md` §3.4.1：新增"Skill / Agent 按 user_intent 过滤"小节

### 🟠 C-4：recursion_limit 安全性

**修改文件**：
- `M1-infrastructure-and-config.md` §1.5：AgentRun 表新增 `node_count` 字段
- `M3.5-agent-runtime-state.md` §1.2：State `node_count` 字段说明
- `M4-langgraph-graph.md` §4.4：execution_loop 节点硬限 200

### 🟠 C-5：user_confirmation 缺少 system 消息

**修改文件**：
- 同 P0-4（interrupt_node 恢复时追加 user_action_msg）

### 🟠 C-6：status_change SSE 高频推送

**修改文件**：
- `M4-langgraph-graph.md` §4.4：execution_loop 节点说明新增"status_change 推送约束"（仅外部可见状态转换）

### 🟠 C-7：JsonPlusSerializer "必须" 说明

**修改文件**：
- `M4-langgraph-graph.md` §6.1：注释改为"显式指定以避免未来版本默认序列化器变更导致不兼容"

### 🟠 C-8：报告邮件发送两条路径冲突

**修改文件**：
- `M5-api-design.md` §3.5：明确 `/api/reports/{date}/email` 仅用于历史补发（不走 confirm），日频流程必须经过 confirm_email 步骤；两条路径共享 idempotency 机制

### 🟠 C-9：Config 与缓存序列化不一致

**修改文件**：
- `M1-infrastructure-and-config.md` §1.3：Config 类新增 `to_json_safe` 静态方法
- `M2-skill-system.md` §5：make_cache_key 改用 `Config.to_json_safe`

### 🟠 F-5：messages 中 system 角色注入时机

**修改文件**：
- 同 P0-4（§3.1 注入点清单 4 个节点）

### 🟠 F-6：confirm 步骤的 params 字段含义

**修改文件**：
- `M3.5-interrupt-mechanism.md` §6：SSE 事件 data.params 携带上下文
- `M5-api-design.md` §3.2：confirm 端点请求体 params 按 confirm_plan / confirm_email 区分
- `M6-frontend-architecture.md` §3.1：confirmationData.type 字段值扩展

### 🟠 F-7：agent 类型步骤的 target 白名单

**修改文件**：
- `M3-agent-architecture.md` §3.3 / §3.4.1：target 字段约束 enum 不含 master
- `M1-infrastructure-and-config.md` §1.5：skill_registry 表 category='agent' 维护白名单

### 🟠 F-8：error_handler 终态定义

**修改文件**：
- `M3.5-agent-runtime-state.md` §1.2：status 终态枚举限定 `completed | failed | cancelled`
- `M4-langgraph-graph.md` §4.9：error_handler 完整实现 + status_reason 字段
- `M6-frontend-architecture.md` §3.1：taskStatus 终态移除 timeout

### 🟠 F-9：缓存失效具体实现

**修改文件**：
- `M2-skill-system.md` §5：缓存失效采用 TTL + 懒删除策略说明

### 🟠 F-10：Agent 与 Skill 在 prompt 中的区分

**修改文件**：
- `M3-agent-architecture.md` §3.4.1：filter_tools_for_intent 函数 + type 标签说明

### 🟠 F-11：线程池与单进程约束

**修改文件**：
- `M3-agent-architecture.md` §3.4.2：新增"单进程线程池约束"小节（uvicorn --workers 1 + SKILL_THREAD_POOL_SIZE=8）

### 🟢 F-12：脱敏不完整

**修改文件**：
- `M1-infrastructure-and-config.md` §1.7：`_summarize_loaded_keys` 改为扁平化遍历 + 敏感关键词列表

### 🟢 F-13：缺少任务状态查询端点

**修改文件**：
- `M5-api-design.md` §2：端点清单 11 → 12（新增 GET /api/tasks/{run_id}/status）
- `M5-api-design.md` §3.2.1：status 端点从 Phase 4 升 MVP
- `M6-frontend-architecture.md` §4.4：重连策略 MVP 实现

### 🟢 F-14：老命名空间映射表存储

**修改文件**：
- `M2-skill-system.md` §6.x：映射表存储位置统一指向 `backend/skills/aliases.py` 常量

### 🟢 C-10：run_id 生成位置

**修改文件**：
- `M4-langgraph-graph.md` §4.1：init 节点 run_id 处理（API 层生成 + 节点复用）
- `M5-api-design.md` §3.1：/api/chat 端点生成 run_id 说明
- `M6-frontend-architecture.md` §3.1：currentRunId 来源说明

### 🟢 C-11：状态术语表

**备注**：术语表建立属于文档体系建设，未在 M1-M6 中新增独立 glossary.md 章节（REVIEW-M3-M6.md §4.2 已有 cross-reference 表）。Phase 2 文档整理阶段统一处理。

---

## ✅ 行动清单

| # | 行动 | 负责方 | 紧急度 | 期望完成 |
|---|------|--------|--------|----------|
| 1 | 应用所有 P0/P1 修改到 7 份设计文档 | software-architect | **P0** | ✅ 2026-06-07 已完成 |
| 2 | 创建 `skill_registry` 表 DDL | software-engineer | **P0** | Phase 2 启动 D1 |
| 3 | 实现 `conversation_history` 写入逻辑（interrupt_node / error_handler） | software-engineer | **P0** | Phase 2 启动 D2 |
| 4 | `agent_runs` 表新增 `node_count / status_reason / checkpoint_retention_hours` 字段 | software-engineer | **P0** | Phase 2 启动 D1 |
| 5 | `skill_executions` 表新增 `step_type / large_payload_ref` 字段 | software-engineer | **P0** | Phase 2 启动 D1 |
| 6 | 每日 04:00 数据库清理脚本（删除 7d 前 agent_runs） | software-engineer | **P0** | Phase 2 启动 D3 |
| 7 | `Config.to_json_safe` + `make_cache_key` 集成测试 | software-engineer | P1 | Phase 2 D5 |
| 8 | `GET /api/tasks/{run_id}/status` 端点实现 | software-engineer | P1 | Phase 2 D7 |
| 9 | 前端 `canResume` 字段 + 1h 窗口判断逻辑 | software-engineer | P1 | Phase 2 D10 |

---

## ⚠️ 待完善 / 已知局限

1. **REVIEW-M3-M6 §五 P1 清单未全部逐条落地**：原文 P1 包含 4 项（端口、SSE 代理、Send API、cancelling 中间态），本轮主要按"用户提供的解决方案"映射 25 项；Send API 与 cancelling 已在 M3 §3.4 和 M3.5 §1.2 中实现，未重复更新。
2. **REVIEW-M3-M6 §五 P2 清单（M5 SSE agent_message、M6 重连策略、M4 step.type 显式禁止）**按"留待编码"原则未在文档中再细化。
3. **glossary.md（术语表）**：C-11 解决方案提出在 `docs/architecture/glossary.md` 建立术语表，**本次未新建独立文件**——M3.5 §4.2 已有 cross-reference 表可满足 MVP 需求，Phase 2 文档整理阶段统一抽出。
4. **REVIEW-M3-M6.md 本身未更新**：原审阅报告中的 P0/P1 清单在本次文档修改中已实质闭环，但审阅报告未追加"已闭环"标注——保留作为历史记录。

---

## 📚 文档修改索引

| 文件 | 修改位置 | 关键变更 |
|------|----------|---------|
| `M1-infrastructure-and-config.md` | §1.3 / §1.5 / §1.7 / §1.10 | Config.to_json_safe、新增 skill_registry + conversation_history 表、_summarize_loaded_keys 扁平化脱敏、新增 M1.9 / M1.10 任务卡片、§1.10.1 数据库清理任务 |
| `M2-skill-system.md` | §5 / §6.x | make_cache_key 用 Config.to_json_safe、缓存失效 TTL+懒删除、别名映射表位置指向 aliases.py |
| `M3-agent-architecture.md` | §3.3 / §3.4 / §3.4.1 / §3.4.2 | 枚举扩展 5 值、Skill/Agent 按 intent 过滤、agent 白名单、Agent vs Skill prompt 区分、单进程线程池约束 |
| `M3.5-agent-runtime-state.md` | §1.1 / §1.2 / §1.3 / §2.2 / §2.3 / §3 / §3.1 / §3.2 | system 消息写入者、状态终态枚举、universe_snapshot transient、checkpoint 双段保留、universe_snapshot 排除实现、system 消息注入 4 个节点、interrupt_consumed 字段 |
| `M3.5-interrupt-mechanism.md` | §3 / §6 / §8.1 | interrupt_data 按 step.type 构造、params 携带上下文、interrupt_node 示例代码追加 system 消息 |
| `M4-langgraph-graph.md` | §4.1 / §4.4 / §4.7 / §4.7.1 / §4.9 / §6.1 / §7 / §8 | run_id API 层生成、node_count 硬限 200、interrupt_node 5 枚举值路由 + system 消息、error_handler 终态实现、JsonPlusSerializer 注释修订、checkpoint 双段保留、计划示例用 confirm_plan/confirm_email |
| `M5-api-design.md` | §2 / §3.1 / §3.2 / §3.2.1 / §3.5 | 端点 11→12（新增 /status）、run_id API 层生成、confirm 请求体 params 区分、status 端点升 MVP、邮件两条路径 + 共享 idempotency |
| `M6-frontend-architecture.md` | §3.1 / §4.4 | messages role 含 system、taskStatus 终态移除 timeout、confirmationData.type 扩展、canResume 字段、刷新页面后调 /status 恢复 UI |

---

> 本报告由软件工坊 AI 主理人根据用户提供的解决方案，对 M1-M6 7 份设计文档进行精确落地修改。架构层 P0/P1 全部闭环，可进入 Phase 3 编码。
